#!upstart
description "BTSync service"

start on filesystem
stop on runlevel [!2345]
respawn

setuid <%= @config["user"] %>
exec "<%= @executable %>" --nodaemon --config "<%= @config["config_file"] %>"
